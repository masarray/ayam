# Revive Chalkboard, Quiz Start, and Menu Close Fix

## What changed
- The explanation board is taller and uses a larger, lighter text style for easier reading.
- `Jawab soal` now makes the quiz overlay visible immediately, then starts loading questions on the next frame. This prevents the stuck blurred-state bug.
- Added `Close` to the menu action list.
- Revive and explanation buttons are now forced into a single full-width column on desktop and mobile.
