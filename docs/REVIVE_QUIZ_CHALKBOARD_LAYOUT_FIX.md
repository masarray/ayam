# Revive Quiz Chalkboard Layout Fix

This update removes the visual explanation renderer and replaces it with a chalkboard-style adaptive text explanation. It also forces revive/quiz action buttons into a stable single-column layout on desktop and mobile, disables background controls while overlays are active, and hardens the revive quiz state transition so the offer card cannot disappear into a blurred empty game state.
